export const metadata = { title: 'Options Screener', description: 'BPS/BCS/IC candidate screener' };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body>{children}</body></html>;
}
